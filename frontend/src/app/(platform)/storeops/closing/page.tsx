// Legacy route — the DM verification view now lives in the Daily Closing module
// (/closing/verify). Re-export the shared component so old links keep working.
export { default } from '@/components/DailyClosingVerify'
