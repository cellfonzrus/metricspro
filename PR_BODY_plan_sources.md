## The POS wizard's "Plans from your carrier data" reads EVERY landed source the org names — per-org config over ONE registry, proposed from the tenant's own words, confirmed, locked (index §23n.1)

**Owner (2026-09-22), verbatim:** *"we have enough plans in the system to bring over but it does not give an option to bring over"* — on the step "Add your plans & features": *"⚡ Plans from your carrier data … 0 record(s) ready to create in pos.service_plans. Neither source has anything yet: your rate-plan catalogue (commcalc.product_mrc) is empty and MetricsPro holds no subscriber report (commcalc.raw_mi) … Bring over 0"*.

### Measured (org `f4f1c16e…`)
- `commcalc.product_mrc` 0 rows · `commcalc.raw_mi` 0 rows — the only two tables `resolve_service_plans` read.
- `commcalc.raw_sales`: **8,350 lines whose category path names a rate plan**, 32 distinct product names ("iPhone Rate Plan (DPA)" 2,632, "New Activation Rate Plan" 558 …) — and under the same branch the rebate lines ("DPA Upgrade iPhone (Rate Plan Rebate)" 1,587, "DPA New Act iPhone (Rate Plan Rebate)" 1,045, "New Activation (Rate Plan Rebate)" 533): leaves `… >> Rate Plans` vs `… >> Rate Plan Rebates`. **A rebate is not a plan.**
- `commcalc.commission_ledger`: 973 + 521 statement lines naming price plans in `product_name`.
- No subscriber report, and maybe never one. The plan names were in the system; the step could not see them.

### THE CLASS, not the instance
**A wizard step derives a tenant fact from a FIXED PAIR of per-feed tables; a tenant whose data lives in another landed source sees "0" and no way to bring anything over.** The 2026-09-08 fix (§23n) widened one table to a pair and left the pair in code. #273 fixed this class for the P&L commission source (a per-org switch, one resolver, suggested then confirmed); #267/#271 fixed it for the activation words (per-org words over the tenant's vocabulary, proposed with counts, the too-broad guard, confirmed). This PR fixes it the same way for plan names.

### What changed (config, one resolver, suggested and confirmed)
| | |
|---|---|
| **ONE registry** | `backend/app/modules/core/plan_sources.py` `HOUSE_SOURCES` — the four landed sources that can carry a plan name, schema-qualified, in precedence order: `catalogue` (`product_mrc`), `subscribers` (`raw_mi`) — ON, today's behaviour byte-identical — and `sales_lines` (`raw_sales`: name `product_desc`, words over `category`/`product_desc`/`department`) and `statement_lines` (`commission_ledger`: name `product_name`, words over `product_name`/`order_type`/`category`) — OFF until the person confirms the words. `onboarding.py` reads no plan table by literal any more; every reader dereferences the registry. |
| **Per-org config, NO migration** | `pos.pos_settings` (mig 725, the POS module's existing per-org kv) org row `key = 'plan_sources'`: `{sources: {<key>: {enabled, include[], exclude[]}}, hints?, broad_attested}`. `plan_sources.resolve_config` merges it over the registry; a tenant can switch a source on/off and declare words, and can NOT add a table. **Decision on the home:** both families named in the brief were checked — `accessory_config` holds sales-LINE classification and would need a new column (a migration the owner would have to apply before the card could save anything); `commission_org_config` is the money-policy row. The POS wizard's fact lives in the POS module's own existing kv, so the card works the day this merges. Every org with no row is byte-identical (pinned). |
| **THE predicate + provenance** | `plan_sources.line_matches` (any configured field contains an include word AND none contains an exclude word; no words → matches nothing, nothing is guessed), `fold_plan_lines`, `plan_rows` — each candidate carries `source`, `source_label`, `lines`, `first_seen`, `last_seen`; a line-level source reports no charge, so `monthly_fee` is None and `mrc_next = "price it under Payout Schedules → Plan MRC"`. Never an invented fee. |
| **The suggestion engine — 2.5a's, REUSED** | `plan_sources.suggest_source` over `line_class.distinct_values` / `candidate_words` (the public name of the 2.5a candidate scan, added) and the same `BROAD_RATIO`: generic include hints matched per field with the count each names; a word naming ≥ 80% of the lines is too broad — reported, never proposed; exclude hints measured over the lines the proposal would take ("'rebate' would leave out 3,165"); `preview` = THE predicate over the proposal. The proposal is the ONLY seed of the card's words. |
| **ONE resolver, both doors** | `onboarding.resolve_service_plans(c, org_id)` reads every configured source (line-level sources are ALWAYS scanned so an off source still reports what it holds and its proposal; harvested only when on and no word in force is refused) and merges in registry precedence. Preview and apply both call it — the P16a invariant kept. `_plan_source_rows` is the one reader of a line-level source; the harvest and the save's guard read the same rows. |
| **The empty state** | `plans_empty_reason` → `plan_sources.empty_sentence`: names every source actually checked and what each held — *"Checked 4 place(s) … Your sales export (line level) (commcalc.raw_sales) — 3,490 line(s) carry a plan word (rate plan), not switched on yet …"* + *"Tick … confirm the words … save, and the plans appear here to bring over."* Never "neither source". |
| **ONE writer + the guard** | `PUT /core/onboarding/import-sources/service_plans_from_product_mrc/config` (gated like apply) → `save_plan_sources`: validates first — a switched-on source whose include word names ≥ 80% of its lines is REFUSED (400 `{message, refused, attest_keys}`, nothing written) unless attested by name (recorded `{by, at, ratio}`); then writes through **`pos/router.upsert_pos_setting`** (factored out of `PUT /pos/settings`, which now delegates to it) and returns the refreshed preview. |
| **The card** | `pos/onboarding/page.tsx` `PlanSourcesCard` + pure `plan-sources-logic.ts`: every source from the payload with table and counts, a tick per source, per line-level source the two word inputs seeded from `suggest.proposal` only, "Found in this file …", "'rebate' would leave out N …", the too-broad note, the refusal with attest-by-name, "Save & re-check" (dirty = differs from what the card last seeded), sample lines with provenance and the MRC pointer, "Bring over N" when N > 0. `apply_import` stays additive; the CSV template path untouched. No table, key or hint word in the frontend. |

### Siblings (checked by name, the 2026-09-20 rule)
- `pos/router._dealer_sync` — the other "bring over" from reports — already reads its table AND column from per-carrier config (mig 293). Not a fixed pair; excused and pinned by the lock.
- `pos/catalog_suggest.own_signals` — reads `pos.activations.plan_description` (the POS's OWN recorded plans, a signal for the catalog presets — a different question) and no landed feed; excused and pinned.
- "Features": there is no separate features table (mig 726) — features are plan rows; no second path.

### Duplicate check (build gate) — what was searched, what is REUSED
Searched index §23n (`resolve_service_plans` / `merge_plan_sources` / `plans_empty_reason`), §16 `product_mrc` / `raw_mi` / `pos.service_plans` / `pos_settings` (mig 725), §17 `/core/onboarding/*` + import-sources preview/apply + `PUT /pos/settings`, §3/§30.12 (`line_class.suggest_rules` / `distinct_values` / `_candidates` / `BROAD_RATIO` / the attestation shape), §4b (`commission_org_config` switch pattern), §32, `catalog_suggest` (`derive_catalog.plans`), `GET /commcalc/product-mrc/coverage`. REUSED: `resolve_service_plans` (extended, still the one resolver), `merge_plan_sources` (varargs), `_catalog_plans` / `_observed_plans` / `fold_subscriber_plans` (unchanged rows), `_page`, `line_class.distinct_values` + `_candidates` (exposed as `candidate_words`) + `BROAD_RATIO`, `pos.pos_settings` + `PUT /pos/settings` (factored into `upsert_pos_setting`), `harness_intake_fakes.FakeDB`, the `apply_import` additive insert, `SERVICE_PLAN_IMPORT_COLS`. NEW: `core/plan_sources.py`, `save_plan_sources` + `CONFIGURABLE_SOURCES` + the `/config` route, `plan_sources_panel`, `PlanSourcesCard` + `plan-sources-logic.ts`, two harness files.

### Lock — `backend/harness_plan_sources_lock.py` (CI, `carrier-vocab-guard.yml`) — 28
(a) one registry, no literal plan table in `onboarding.py`, a tenant cannot add a table · (b) one resolver: preview + apply, no second derivation, the guard reads the same rows · (c) one home, one writer, the save measures BEFORE it writes, no migration · (d) "Neither source" gone from the code · (e) the card renders the payload's sources, saves through `/config`, seeds from the proposal; no table / key / hint in the frontend; the proof exists; CI runs the lock; the vocab guard scans the module · (f) RULE TWO over the module, the logic file and the card (the guard's own derived vocabulary) · (g) both siblings pinned · (h) 8 negative controls.

### Proof (DB-free)
- `backend/harness_pos_onboarding.py` **193 → 231** (§P17): the measured-shape tenant (1:10) → 0 under house defaults with an empty state naming all four sources; "rate plan" proposed (665 lines), "rebate" as the exclusion (removes 316), preview 349 lines / 3 names; after confirming: 5 plans (3 + 2) with provenance, no rebate, no invented fee, most-sold first; saved as ONE org-level `pos_settings` row; apply creates 5 org-stamped rows, re-run 0 created / 5 skipped; the other tenant's rows never leak; a word naming 90% REFUSED with nothing written, attested by name → saved with who / when / share; **the PIN** — a catalogue + subscriber tenant's rows byte-identical, diag keys kept, a house source switchable off, an unknown key ignored, per-source merge.
- `frontend/prove_plan_sources_card.mjs` **20** — the real logic file over the real backend payload.
- Existing green: `harness_pos_onboarding` (all prior sections), `harness_line_class` 47 + lock 29, `harness_onboarding_intake` 164 / 87 / 92 / 49, `harness_carrier_vocab_guard`, the report-kind / landing-identity / POS-sales locks, `tsc --noEmit`.

### What the owner will see on the measured tenant
Four sources on the card: catalogue 0 · subscribers 0 · **sales export ≈ 48,875 lines — "rate plan" proposed (8,350), "rebate" proposed as the exclusion (3,165)**, preview ≈ 5,185 lines and the non-rebate names among the 32 (the three measured rebate names drop out; any other name carrying "rebate" drops with them) · **commission statement 1,494 lines** with its own proposal. Clicks: tick the sales export (and the statement if its proposal reads right) → **Save & re-check** → **Bring over N**. Those rows carry no monthly charge; each says where to price it.

**Migrations: none.** **Money: none** — the wizard seeds `pos.service_plans` names; nothing is computed from them. Registered in index §23n.1, §16 (`pos.pos_settings`, `product_mrc`, `raw_mi`, `pos.service_plans`), §17, §18 and `docs/ONBOARDING_FLOW_DESIGN.md` §13.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01Ybsehpbqn5E2oKoGGR4uhf
